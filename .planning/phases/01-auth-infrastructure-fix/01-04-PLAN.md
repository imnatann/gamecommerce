---
phase: 01-auth-infrastructure-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/Seller/KycController.php
  - app/Http/Requests/Seller/KycSubmissionRequest.php
  - routes/seller.php
  - resources/views/seller/kyc/verify.blade.php
autonomous: true
requirements:
  - REQ-auth-kyc
  - REQ-nfr-security

must_haves:
  truths:
    - "Seller yang belum KYC dapat mengakses GET /seller/kyc tanpa redirect loop"
    - "Route seller.kyc.verify ada dan di-resolve oleh Route::has('seller.kyc.verify')"
    - "POST /seller/kyc dengan data valid mengubah kyc_status ke 'pending'"
    - "NIK dengan bukan 16 digit gagal validasi dengan pesan error"
    - "File upload KTP dan selfie disimpan via spatie/medialibrary"
  artifacts:
    - path: "app/Http/Controllers/Seller/KycController.php"
      provides: "KYC form show + submit handler"
      exports: ["index", "store"]
    - path: "app/Http/Requests/Seller/KycSubmissionRequest.php"
      provides: "Validated KYC payload"
      exports: ["rules", "authorize"]
    - path: "routes/seller.php"
      provides: "KYC route registration"
      contains: "seller.kyc.verify"
    - path: "resources/views/seller/kyc/verify.blade.php"
      provides: "KYC form Blade view"
      contains: "route('seller.kyc.store')"
  key_links:
    - from: "app/Http/Middleware/EnsureKycVerified.php:31"
      to: "routes/seller.php"
      via: "Route::has('seller.kyc.verify')"
      pattern: "seller.kyc.verify"
    - from: "routes/seller.php"
      to: "app/Http/Controllers/Seller/KycController.php"
      via: "Route::get('/kyc') name('kyc.verify')"
      pattern: "KycController"
---

<objective>
Buat KYC controller + form request + route + Blade view untuk seller verification flow. Route seller.kyc.verify HARUS ada agar EnsureKycVerified middleware dapat redirect seller ke halaman KYC.

Purpose: REQ-auth-kyc — saat ini `Route::has('seller.kyc.verify')` di EnsureKycVerified.php line 31 selalu false, sehingga unverified seller di-redirect ke halaman yang tidak ada. Gap kritis yang memblokir semua seller.
Output: KycController + KycSubmissionRequest + seller route update + KYC Blade view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-auth-infrastructure-fix/01-RESEARCH.md
@.planning/phases/01-auth-infrastructure-fix/01-PATTERNS.md

<interfaces>
<!-- Analog: app/Http/Controllers/Seller/SettingsController.php — exact pattern match -->
```php
namespace App\Http\Controllers\Seller;
use App\Http\Controllers\Controller;
use Illuminate\Support\Facades\Auth;  // WAJIB: Auth::user(), bukan auth()->user()

class SettingsController extends Controller
{
    public function index()                   // no return type
    {
        $seller = Auth::user();               // Auth facade, bukan auth() helper
        return view('seller.settings', compact('seller'));
    }
    // Flash: back()->with('success', 'Pesan bahasa Indonesia')
}
```

<!-- User::registerMediaCollections() harus ditambahkan jika belum ada 'kyc_id_photo' collection -->
```php
// app/Models/User.php line 72-75:
public function registerMediaCollections(): void
{
    $this->addMediaCollection('avatar')->singleFile();
    // 'kyc_id_photo' dan 'kyc_selfie' belum terdaftar — perlu ditambahkan
}
```

<!-- routes/seller.php current state — routes DITAMBAHKAN, tidak diganti -->
// Semua route mendapat prefix 'seller.' otomatis dari bootstrap/app.php
// Route::get('/kyc') -> name: 'seller.kyc.verify'  (KRITIS: EnsureKycVerified cek ini)
// Route::post('/kyc') -> name: 'seller.kyc.store'

<!-- KYC media collections yang akan digunakan -->
// addMediaFromRequest('id_photo')->toMediaCollection('kyc_id_photo')
// addMediaFromRequest('selfie_photo')->toMediaCollection('kyc_selfie')
// Koleksi ini perlu didaftarkan di User::registerMediaCollections()

<!-- Blade patterns dari resources/views/seller/settings.blade.php -->
// Layout: @extends('layouts.seller', ['title' => '...'])
// CSS: var(--color-gc-*), gc-card, gc-btn gc-btn-primary gc-btn-md
// Script: @push('scripts') ... @endpush (BUKAN @section('scripts'))
// Alpine: <div x-data="componentName()"> wrapping seluruh konten
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat KycSubmissionRequest dan KycController</name>
  <files>
    app/Http/Requests/Seller/KycSubmissionRequest.php,
    app/Http/Controllers/Seller/KycController.php,
    app/Models/User.php
  </files>
  <behavior>
    - KycSubmissionRequest::rules() validasi: full_name required|string|max:255, id_number required|string|size:16, id_photo required|image|max:5120|mimes:jpg,jpeg,png, selfie_photo required|image|max:5120|mimes:jpg,jpeg,png, bank_name required|string|max:100, bank_account required|string|max:50, bank_holder required|string|max:255, npwp_number nullable|string|max:20
    - KycController::index() → tampilkan view seller.kyc.verify dengan user data
    - KycController::store() → validasi via KycSubmissionRequest, upload media via medialibrary, update kyc_status ke 'pending', simpan meta data, redirect ke seller.dashboard
    - User::registerMediaCollections() ditambahkan kyc_id_photo dan kyc_selfie collections
  </behavior>
  <action>
**app/Http/Requests/Seller/KycSubmissionRequest.php**

CATATAN: Meski PATTERNS.md menyatakan Seller controller tidak menggunakan FormRequest, KYC submission memerlukan validasi kompleks + file upload yang lebih bersih dengan FormRequest. Ini adalah deviation yang justified.

```php
<?php

namespace App\Http\Requests\Seller;

use Illuminate\Foundation\Http\FormRequest;

class KycSubmissionRequest extends FormRequest
{
    public function authorize(): bool
    {
        $user = $this->user();
        return $user && $user->isSeller() && ! $user->isKycVerified();
    }

    public function rules(): array
    {
        return [
            'full_name'    => ['required', 'string', 'max:255'],
            'id_number'    => ['required', 'string', 'size:16'],
            'id_photo'     => ['required', 'image', 'max:5120', 'mimes:jpg,jpeg,png'],
            'selfie_photo' => ['required', 'image', 'max:5120', 'mimes:jpg,jpeg,png'],
            'bank_name'    => ['required', 'string', 'max:100'],
            'bank_account' => ['required', 'string', 'max:50'],
            'bank_holder'  => ['required', 'string', 'max:255'],
            'npwp_number'  => ['nullable', 'string', 'max:20'],
        ];
    }

    public function messages(): array
    {
        return [
            'id_number.size' => 'Nomor KTP harus tepat 16 digit.',
            'id_photo.required' => 'Foto KTP wajib diupload.',
            'selfie_photo.required' => 'Foto selfie dengan KTP wajib diupload.',
        ];
    }
}
```

**app/Http/Controllers/Seller/KycController.php**
```php
<?php

namespace App\Http\Controllers\Seller;

use App\Http\Controllers\Controller;
use App\Http\Requests\Seller\KycSubmissionRequest;
use Illuminate\Support\Facades\Auth;

class KycController extends Controller
{
    public function index()
    {
        $seller = Auth::user();

        return view('seller.kyc.verify', compact('seller'));
    }

    public function store(KycSubmissionRequest $request)
    {
        $seller    = Auth::user();
        $validated = $request->validated();

        // Upload foto KTP via spatie/medialibrary
        $seller->addMediaFromRequest('id_photo')
            ->toMediaCollection('kyc_id_photo');

        // Upload foto selfie via spatie/medialibrary
        $seller->addMediaFromRequest('selfie_photo')
            ->toMediaCollection('kyc_selfie');

        // Update status dan simpan data KYC ke meta
        $seller->update([
            'kyc_status' => 'pending',
            'meta'       => array_merge($seller->meta ?? [], [
                'kyc_full_name'    => $validated['full_name'],
                'kyc_id_number'    => $validated['id_number'],
                'kyc_bank_name'    => $validated['bank_name'],
                'kyc_bank_account' => $validated['bank_account'],
                'kyc_bank_holder'  => $validated['bank_holder'],
                'kyc_npwp'         => $validated['npwp_number'] ?? null,
                'kyc_submitted_at' => now()->toIso8601String(),
            ]),
        ]);

        return redirect()->route('seller.dashboard')
            ->with('success', 'Dokumen KYC berhasil dikirim. Tim kami akan memverifikasi dalam 1-2 hari kerja.');
    }
}
```

**Tambahan ke app/Models/User.php** — update registerMediaCollections() untuk menambahkan KYC collections:

Cari method `registerMediaCollections()` (saat ini line 72-75) dan update:
```php
public function registerMediaCollections(): void
{
    $this->addMediaCollection('avatar')->singleFile();
    $this->addMediaCollection('kyc_id_photo')->singleFile();
    $this->addMediaCollection('kyc_selfie')->singleFile();
}
```
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php -l app/Http/Requests/Seller/KycSubmissionRequest.php && php -l app/Http/Controllers/Seller/KycController.php && echo "Syntax OK"</automated>
  </verify>
  <done>Kedua file ada; syntax valid; User::registerMediaCollections() berisi kyc_id_photo dan kyc_selfie collections.</done>
</task>

<task type="auto">
  <name>Task 2: Daftarkan KYC routes di seller.php</name>
  <files>routes/seller.php</files>
  <action>
Edit routes/seller.php — tambahkan import dan dua route KYC DI BAGIAN ATAS sebelum route lain.

KRITIS: Route ini HARUS bernama 'kyc.verify' (akan menjadi 'seller.kyc.verify' dengan prefix otomatis dari bootstrap/app.php). EnsureKycVerified.php line 31 mencari tepat 'seller.kyc.verify'.

Route ini berada di dalam bootstrap/app.php middleware group `['auth', 'seller']` — sudah benar, TIDAK perlu middleware 'kyc' tambahan. Seller yang belum KYC masih bisa akses route ini.

Tambahkan di bagian atas file setelah use statements yang ada:
```php
use App\Http\Controllers\Seller\KycController;
```

Tambahkan route SEBELUM Route::get('/dashboard') yang sudah ada:
```php
// KYC verification — HARUS di luar middleware 'kyc' (accessible oleh unverified seller)
Route::get('/kyc', [KycController::class, 'index'])->name('kyc.verify');
Route::post('/kyc', [KycController::class, 'store'])->name('kyc.store');
```

File akhir seller.php harus memiliki semua route yang sudah ada PLUS dua route KYC di atas.
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php artisan route:list --name=seller.kyc 2>&1 | grep -c "kyc"</automated>
  </verify>
  <done>php artisan route:list --name=seller.kyc menampilkan minimal 2 routes (kyc.verify dan kyc.store); Route::has('seller.kyc.verify') mengembalikan true.</done>
</task>

<task type="auto">
  <name>Task 3: Buat KYC Blade view</name>
  <files>resources/views/seller/kyc/verify.blade.php</files>
  <action>
Buat direktori resources/views/seller/kyc/ dan file verify.blade.php. Ikuti pola EXACT dari resources/views/seller/settings.blade.php:
- Layout: @extends('layouts.seller', ...)
- CSS: gc-* utility classes dan var(--color-gc-*) tokens
- Alpine.js: @push('scripts'), bukan @section('scripts')
- Button classes: gc-btn gc-btn-primary gc-btn-md

```blade
@extends('layouts.seller', ['title' => 'Verifikasi KYC'])

@section('content')
<div x-data="kycForm()" class="max-w-3xl mx-auto">

    {{-- Page header --}}
    <div class="mb-6">
        <h1 class="font-[var(--font-family-display)] text-2xl lg:text-3xl font-bold text-[var(--color-gc-text)]">
            Verifikasi KYC
        </h1>
        <p class="text-sm text-[var(--color-gc-text-secondary)] mt-1">
            Lengkapi verifikasi identitas untuk mulai berjualan di GameCommerce
        </p>
    </div>

    {{-- Status info --}}
    @if($seller->kyc_status === 'pending')
        <div class="gc-card p-4 mb-6 border border-[var(--color-gc-warning)] bg-[var(--color-gc-warning)]/10">
            <p class="text-sm text-[var(--color-gc-warning)] font-medium">
                Dokumen KYC Anda sedang diproses. Harap tunggu 1-2 hari kerja.
            </p>
        </div>
    @elseif($seller->kyc_status === 'rejected')
        <div class="gc-card p-4 mb-6 border border-[var(--color-gc-error)] bg-[var(--color-gc-error)]/10">
            <p class="text-sm text-[var(--color-gc-error)] font-medium">
                Verifikasi KYC ditolak. Silakan ajukan kembali dengan dokumen yang valid.
            </p>
        </div>
    @endif

    <form method="POST" action="{{ route('seller.kyc.store') }}" enctype="multipart/form-data">
        @csrf

        {{-- Data Identitas --}}
        <div class="gc-card p-6 mb-4">
            <h2 class="font-[var(--font-family-display)] text-lg font-semibold text-[var(--color-gc-text)] mb-4">
                Data Identitas
            </h2>

            <div class="space-y-4">
                <div>
                    <label for="full_name" class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                        Nama Lengkap (sesuai KTP)
                    </label>
                    <input type="text" id="full_name" name="full_name"
                           value="{{ old('full_name') }}"
                           class="gc-input w-full @error('full_name') border-[var(--color-gc-error)] @enderror"
                           placeholder="Nama sesuai KTP"
                           required>
                    @error('full_name')
                        <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                    @enderror
                </div>

                <div>
                    <label for="id_number" class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                        Nomor KTP (NIK — 16 digit)
                    </label>
                    <input type="text" id="id_number" name="id_number"
                           value="{{ old('id_number') }}"
                           class="gc-input w-full @error('id_number') border-[var(--color-gc-error)] @enderror"
                           placeholder="1234567890123456"
                           maxlength="16"
                           pattern="[0-9]{16}"
                           required>
                    @error('id_number')
                        <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                    @enderror
                </div>
            </div>
        </div>

        {{-- Dokumen Foto --}}
        <div class="gc-card p-6 mb-4">
            <h2 class="font-[var(--font-family-display)] text-lg font-semibold text-[var(--color-gc-text)] mb-4">
                Dokumen Foto
            </h2>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {{-- Foto KTP --}}
                <div>
                    <label class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                        Foto KTP <span class="text-[var(--color-gc-error)]">*</span>
                    </label>
                    <div x-data="{ preview: null }"
                         class="border-2 border-dashed border-[var(--color-gc-border)] rounded-lg p-4 text-center">
                        <input type="file" name="id_photo"
                               x-ref="idPhoto"
                               @change="preview = URL.createObjectURL($event.target.files[0])"
                               accept="image/jpg,image/jpeg,image/png"
                               class="hidden"
                               required>
                        <template x-if="!preview">
                            <div @click="$refs.idPhoto.click()" class="cursor-pointer">
                                <p class="text-sm text-[var(--color-gc-text-secondary)]">Klik untuk upload foto KTP</p>
                                <p class="text-xs text-[var(--color-gc-text-muted)] mt-1">JPG, PNG, maks 5MB</p>
                            </div>
                        </template>
                        <template x-if="preview">
                            <div>
                                <img :src="preview" class="max-h-32 mx-auto rounded" alt="Preview KTP">
                                <button type="button" @click="preview = null; $refs.idPhoto.value = ''"
                                        class="text-xs text-[var(--color-gc-error)] mt-2">Hapus</button>
                            </div>
                        </template>
                    </div>
                    @error('id_photo')
                        <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                    @enderror
                </div>

                {{-- Foto Selfie --}}
                <div>
                    <label class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                        Foto Selfie dengan KTP <span class="text-[var(--color-gc-error)]">*</span>
                    </label>
                    <div x-data="{ preview: null }"
                         class="border-2 border-dashed border-[var(--color-gc-border)] rounded-lg p-4 text-center">
                        <input type="file" name="selfie_photo"
                               x-ref="selfiePhoto"
                               @change="preview = URL.createObjectURL($event.target.files[0])"
                               accept="image/jpg,image/jpeg,image/png"
                               class="hidden"
                               required>
                        <template x-if="!preview">
                            <div @click="$refs.selfiePhoto.click()" class="cursor-pointer">
                                <p class="text-sm text-[var(--color-gc-text-secondary)]">Klik untuk upload foto selfie</p>
                                <p class="text-xs text-[var(--color-gc-text-muted)] mt-1">JPG, PNG, maks 5MB</p>
                            </div>
                        </template>
                        <template x-if="preview">
                            <div>
                                <img :src="preview" class="max-h-32 mx-auto rounded" alt="Preview Selfie">
                                <button type="button" @click="preview = null; $refs.selfiePhoto.value = ''"
                                        class="text-xs text-[var(--color-gc-error)] mt-2">Hapus</button>
                            </div>
                        </template>
                    </div>
                    @error('selfie_photo')
                        <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                    @enderror
                </div>
            </div>
        </div>

        {{-- Data Rekening Bank --}}
        <div class="gc-card p-6 mb-4">
            <h2 class="font-[var(--font-family-display)] text-lg font-semibold text-[var(--color-gc-text)] mb-4">
                Rekening Bank (untuk pencairan dana)
            </h2>

            <div class="space-y-4">
                <div>
                    <label for="bank_name" class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                        Nama Bank
                    </label>
                    <input type="text" id="bank_name" name="bank_name"
                           value="{{ old('bank_name') }}"
                           class="gc-input w-full @error('bank_name') border-[var(--color-gc-error)] @enderror"
                           placeholder="Contoh: BCA, Mandiri, BNI"
                           required>
                    @error('bank_name')
                        <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                    @enderror
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label for="bank_account" class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                            Nomor Rekening
                        </label>
                        <input type="text" id="bank_account" name="bank_account"
                               value="{{ old('bank_account') }}"
                               class="gc-input w-full @error('bank_account') border-[var(--color-gc-error)] @enderror"
                               placeholder="1234567890"
                               required>
                        @error('bank_account')
                            <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                        @enderror
                    </div>

                    <div>
                        <label for="bank_holder" class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                            Nama Pemilik Rekening
                        </label>
                        <input type="text" id="bank_holder" name="bank_holder"
                               value="{{ old('bank_holder') }}"
                               class="gc-input w-full @error('bank_holder') border-[var(--color-gc-error)] @enderror"
                               placeholder="Sesuai nama di buku tabungan"
                               required>
                        @error('bank_holder')
                            <p class="text-xs text-[var(--color-gc-error)] mt-1">{{ $message }}</p>
                        @enderror
                    </div>
                </div>
            </div>
        </div>

        {{-- NPWP Opsional --}}
        <div class="gc-card p-6 mb-6">
            <h2 class="font-[var(--font-family-display)] text-lg font-semibold text-[var(--color-gc-text)] mb-2">
                NPWP <span class="text-xs font-normal text-[var(--color-gc-text-secondary)]">(Opsional)</span>
            </h2>
            <p class="text-xs text-[var(--color-gc-text-secondary)] mb-4">
                Diperlukan jika pendapatan melebihi Rp 4.8 juta/tahun (ketentuan DJP).
            </p>
            <div>
                <label for="npwp_number" class="block text-sm font-medium text-[var(--color-gc-text-secondary)] mb-1">
                    Nomor NPWP
                </label>
                <input type="text" id="npwp_number" name="npwp_number"
                       value="{{ old('npwp_number') }}"
                       class="gc-input w-full"
                       placeholder="00.000.000.0-000.000">
            </div>
        </div>

        {{-- Submit --}}
        <div class="flex justify-end gap-3">
            <a href="{{ route('seller.dashboard') }}"
               class="gc-btn gc-btn-ghost gc-btn-md">
                Batal
            </a>
            <button type="submit" class="gc-btn gc-btn-primary gc-btn-md">
                Kirim untuk Verifikasi
            </button>
        </div>

    </form>

</div>
@endsection

@push('scripts')
<script>
Alpine.data('kycForm', () => ({
    // Alpine component placeholder — file preview ditangani inline di setiap input
    init() {
        // Component initialization jika diperlukan
    },
}));
</script>
@endpush
```
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php artisan route:list --name=seller.kyc 2>&1 | grep -c "kyc" && grep -c "seller.kyc.store" resources/views/seller/kyc/verify.blade.php</automated>
  </verify>
  <done>View file ada di resources/views/seller/kyc/verify.blade.php; route seller.kyc.verify dan seller.kyc.store ada di route list; view menggunakan route('seller.kyc.store') di form action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → KYC form | File upload dari user — harus divalidasi mime + size sebelum disimpan |
| KYC store → medialibrary | File path tidak boleh keluar dari storage yang dikonfigurasi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | KYC file upload mime spoofing | mitigate | `mimes:jpg,jpeg,png` di FormRequest + spatie/medialibrary melakukan mime detection server-side — tidak mengandalkan extension saja |
| T-01-04-02 | Tampering | KYC file upload size | mitigate | `max:5120` (5MB) di FormRequest mencegah DoS via large file upload |
| T-01-04-03 | Elevation of Privilege | KYC re-submission oleh verified seller | mitigate | KycSubmissionRequest::authorize() mengecek `!isKycVerified()` — verified seller tidak bisa override status |
| T-01-04-04 | Information Disclosure | NIK 16 digit tersimpan di meta JSON | accept | meta column di-cast ke array, tidak di-expose ke API publik; akses via admin panel yang protected |
| T-01-04-05 | Tampering | CSRF pada KYC form | mitigate | @csrf directive di form — Laravel VerifyCsrfToken aktif di web middleware |
</threat_model>

<verification>
```bash
# Route terdaftar
php artisan route:list --name=seller.kyc

# Verifikasi nama route kritis untuk EnsureKycVerified
php artisan tinker --execute="echo Route::has('seller.kyc.verify') ? 'FOUND' : 'MISSING';"

# Syntax check semua file baru
cd /Users/tokaf/gamecommerce
php -l app/Http/Requests/Seller/KycSubmissionRequest.php
php -l app/Http/Controllers/Seller/KycController.php

# View file ada
ls resources/views/seller/kyc/verify.blade.php

# KYC test
php artisan test --filter KycSubmissionTest
```
</verification>

<success_criteria>
- Route seller.kyc.verify ada: `php artisan route:list --name=seller.kyc.verify` tidak kosong
- Route seller.kyc.store ada: `php artisan route:list --name=seller.kyc.store` tidak kosong
- KycController::store() mengupdate kyc_status ke 'pending'
- KycSubmissionRequest::rules() menolak id_number yang bukan 16 karakter
- View ada dan menggunakan @csrf, enctype="multipart/form-data", route('seller.kyc.store')
- php artisan test --filter KycSubmissionTest: semua 4 test PASS
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/01-auth-infrastructure-fix/01-04-SUMMARY.md`
</output>
