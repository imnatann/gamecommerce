---
phase: 01-auth-infrastructure-fix
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/Web/LocaleController.php
  - routes/web.php
  - app/Models/User.php
autonomous: true
requirements:
  - REQ-nfr-localization
  - REQ-auth-routes

must_haves:
  truths:
    - "POST /locale/en mengubah session('locale') ke 'en'"
    - "POST /locale/id mengubah session('locale') ke 'id'"
    - "POST /locale/fr mengembalikan 404"
    - "Route locale.switch ada dan di-resolve oleh Route::has('locale.switch')"
    - "User::isKycVerified() mengembalikan true hanya jika kyc_status raw value adalah 'verified'"
    - "getKycStatusAttribute() tidak lagi me-remap 'verified' ke 'approved'"
    - "User::registerMediaCollections() berisi kyc_id_photo dan kyc_selfie collections (digunakan oleh Plan 04 KycController)"
  artifacts:
    - path: "app/Http/Controllers/Web/LocaleController.php"
      provides: "Locale switching endpoint"
      exports: ["switch"]
    - path: "routes/web.php"
      provides: "Locale route registration"
      contains: "locale.switch"
    - path: "app/Models/User.php"
      provides: "KYC accessor refactor + media collections (kyc_id_photo, kyc_selfie)"
      contains: "isKycVerified, registerMediaCollections"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/Web/LocaleController.php"
      via: "Route::post('/locale/{locale}')"
      pattern: "LocaleController"
    - from: "app/Http/Middleware/SetLocale.php"
      to: "session('locale')"
      via: "LocaleController::switch() writes session key"
      pattern: "locale"
---

<objective>
Buat LocaleController + locale route untuk session-only locale switching, dan refactor User::getKycStatusAttribute() untuk menghapus mapping 'verified' → 'approved' yang menyebabkan inkonsistensi.

Purpose: REQ-nfr-localization (locale switching endpoint) dan gap #11 dari STATE.md (KYC accessor inconsistency). Saat ini tidak ada endpoint untuk switching bahasa, dan `isKycVerified()` bergantung pada dua nilai yang membingungkan.
Output: LocaleController + web.php update + User model accessor refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-auth-infrastructure-fix/01-RESEARCH.md
@.planning/phases/01-auth-infrastructure-fix/01-PATTERNS.md

<interfaces>
<!-- app/Http/Middleware/SetLocale.php — yang MEMBACA dari session -->
```php
// SetLocale middleware sudah membaca session('locale') sebagai sumber pertama
// LocaleController hanya perlu MENULIS ke session('locale')
// Supported locales: ['id', 'en'] — harus sesuai dengan yang didukung SetLocale
```

<!-- Analog: app/Http/Controllers/Web/HomeController.php — namespace pattern -->
```php
namespace App\Http\Controllers\Web;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;
```

<!-- routes/web.php current state — locale route ditambahkan SEBELUM Route::fallback() -->
// Route locale bisa diakses tanpa auth (user tamu juga perlu ganti bahasa)
// Pattern: Route::post('/locale/{locale}', [...]) dengan parameter di URL, bukan di body

<!-- User::getKycStatusAttribute() SAAT INI (harus direfactor) -->
```php
// BEFORE (line 84-87):
public function getKycStatusAttribute(?string $value): ?string
{
    return $value === 'verified' ? 'approved' : $value;
}
// Problem: DB menyimpan 'verified', tapi accessor mengembalikan 'approved'
// isKycVerified() checks both ['verified', 'approved'] sebagai workaround

// AFTER (sesuai keputusan dari ANSWERS TO RESEARCHER):
// Hapus remapping — kembalikan value raw tanpa transformasi
// isKycVerified() cukup cek 'verified' saja (nilai yang ada di DB)
```

<!-- PENTING: SellerPolicy::submitKyc() di Plan 02 menggunakan getRawOriginal() untuk menghindari accessor -->
// Setelah refactor ini, getRawOriginal() tidak diperlukan lagi tapi tidak merusak behavior
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat LocaleController dan daftarkan locale route</name>
  <files>
    app/Http/Controllers/Web/LocaleController.php,
    routes/web.php
  </files>
  <behavior>
    - LocaleController::switch() menerima string $locale dari route parameter
    - Validasi: hanya 'id' dan 'en' yang valid; selain itu → abort(404)
    - Menulis ke session('locale') dengan nilai yang valid
    - Redirect back() dengan flash 'success'
    - Route name: 'locale.switch', method: POST, path: '/locale/{locale}'
    - Route bisa diakses tanpa auth (tamu juga perlu ganti bahasa)
  </behavior>
  <action>
**app/Http/Controllers/Web/LocaleController.php**
```php
<?php

namespace App\Http\Controllers\Web;

use App\Http\Controllers\Controller;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;

class LocaleController extends Controller
{
    /**
     * Supported locales — harus sesuai dengan SetLocale middleware.
     */
    private const SUPPORTED = ['id', 'en'];

    public function switch(Request $request, string $locale): RedirectResponse
    {
        if (! in_array($locale, self::SUPPORTED, true)) {
            abort(404);
        }

        session()->put('locale', $locale);

        return redirect()->back()->with('success', 'Bahasa berhasil diubah.');
    }
}
```

**routes/web.php** — tambahkan import dan route SEBELUM Route::fallback() di akhir file.

Tambahkan import di bagian atas (setelah use statements yang ada):
```php
use App\Http\Controllers\Web\LocaleController;
```

Tambahkan route SEBELUM `Route::fallback(...)`:
```php
// Locale switcher — tidak memerlukan auth, session-only
Route::post('/locale/{locale}', [LocaleController::class, 'switch'])->name('locale.switch');
```
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php artisan route:list --name=locale.switch 2>&1 | grep -c "locale" && php -l app/Http/Controllers/Web/LocaleController.php && echo "OK"</automated>
  </verify>
  <done>Route locale.switch ada di route list; LocaleController syntax valid; route accessible tanpa auth middleware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor User KYC accessor — hapus 'verified' → 'approved' remapping</name>
  <files>app/Models/User.php</files>
  <behavior>
    - getKycStatusAttribute() dikembalikan ke raw value tanpa transformasi (hapus 'verified'→'approved' mapping)
    - isKycVerified() diupdate untuk hanya cek 'verified' (bukan lagi array ['verified','approved'])
    - Tidak ada perubahan schema — DB sudah menyimpan 'verified' sebagai nilai canonical
    - KycSubmissionRequest::authorize() dan SellerPolicy::submitKyc() tetap benar karena keduanya menggunakan isKycVerified() atau raw value check
  </behavior>
  <action>
Edit app/Models/User.php — TIGA perubahan (semua di file yang sama, satu commit):

**Perubahan 1: getKycStatusAttribute()** (sekitar line 84)

BEFORE:
```php
public function getKycStatusAttribute(?string $value): ?string
{
    return $value === 'verified' ? 'approved' : $value;
}
```

AFTER — hapus remapping, kembalikan raw value:
```php
public function getKycStatusAttribute(?string $value): ?string
{
    return $value;
}
```

**Perubahan 2: isKycVerified()** (sekitar line 170)

BEFORE:
```php
public function isKycVerified(): bool
{
    return in_array($this->kyc_status, ['verified', 'approved'], true);
}
```

AFTER — hanya cek 'verified' (DB canonical value):
```php
public function isKycVerified(): bool
{
    return $this->getRawOriginal('kyc_status') === 'verified';
}
```

CATATAN: Menggunakan `getRawOriginal('kyc_status')` daripada `$this->kyc_status` agar bahkan jika accessor masih ada (atau ada accessor lain di masa depan), kita selalu cek nilai DB langsung. Ini defensive programming yang tidak menambah overhead.

**Perubahan 3: registerMediaCollections() — tambah KYC collections** (sekitar line 72-75)

Plan 04 (KycController) memerlukan collections `kyc_id_photo` dan `kyc_selfie` untuk upload via spatie/medialibrary. Plan 05 memiliki ownership penuh atas app/Models/User.php (mencegah Wave 1 race condition), sehingga edit ini ada di sini.

BEFORE:
```php
public function registerMediaCollections(): void
{
    $this->addMediaCollection('avatar')->singleFile();
}
```

AFTER:
```php
public function registerMediaCollections(): void
{
    $this->addMediaCollection('avatar')->singleFile();
    $this->addMediaCollection('kyc_id_photo')->singleFile();
    $this->addMediaCollection('kyc_selfie')->singleFile();
}
```

CATATAN PENTING: Setelah perubahan ini, semua tempat yang mengecek `$user->kyc_status === 'approved'` akan berubah behavior. Search codebase untuk 'approved' setelah selesai untuk memastikan tidak ada code yang bergantung pada nilai lama.

Jalankan search ini setelah edit:
```bash
grep -rn "'approved'" /Users/tokaf/gamecommerce/app/ --include="*.php"
```
Jika ada hasil selain definisi lama yang sudah dihapus, update ke 'verified'.
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php artisan tinker --execute="echo App\Models\User::factory()->make(['kyc_status' => 'verified'])->isKycVerified() ? 'PASS:verified' : 'FAIL:verified';" 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>User::isKycVerified() mengembalikan true untuk kyc_status='verified'; getKycStatusAttribute() mengembalikan nilai raw tanpa transformasi; grep untuk 'approved' di codebase tidak ada false positive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → LocaleController | Route tidak memerlukan auth — user tamu bisa set locale |
| LocaleController → session | Nilai locale ditulis ke session — harus divalidasi sebelum disimpan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Locale route — arbitrary locale injection | mitigate | Whitelist check `in_array($locale, ['id','en'])` sebelum write ke session; invalid locale → abort(404) |
| T-01-05-02 | Information Disclosure | KYC status raw value di-expose | accept | kyc_status bukan data sensitif — hanya 'pending'/'verified'/'rejected'. Nilai ini sudah ada di User model sebelum refactor |
| T-01-05-03 | Tampering | Locale sebagai URL param (path injection) | mitigate | Route parameter di-bind langsung sebagai PHP string parameter — tidak ada path traversal risk karena tidak ada filesystem operation |
| T-01-05-04 | Regression | isKycVerified() behavior change | mitigate | Refactor ke getRawOriginal() memastikan DB value selalu dicek, menghilangkan ambiguity antara 'verified' dan 'approved' |
</threat_model>

<verification>
```bash
# Route ada
cd /Users/tokaf/gamecommerce && php artisan route:list --name=locale.switch

# Syntax check
php -l app/Http/Controllers/Web/LocaleController.php
php -l app/Models/User.php

# KYC refactor verification via tinker
php artisan tinker --execute="
\$u = App\Models\User::factory()->make(['kyc_status' => 'verified']);
echo 'verified->isKycVerified: ' . (\$u->isKycVerified() ? 'true' : 'false') . PHP_EOL;
echo 'verified->kyc_status raw: ' . \$u->getRawOriginal('kyc_status') . PHP_EOL;
\$u2 = App\Models\User::factory()->make(['kyc_status' => 'approved']);
echo 'approved->isKycVerified: ' . (\$u2->isKycVerified() ? 'true' : 'false') . PHP_EOL;
"

# Search untuk 'approved' yang mungkin perlu update
grep -rn "'approved'" /Users/tokaf/gamecommerce/app/ --include="*.php"

# Locale switch tests
php artisan test --filter LocaleSwitchTest
```
</verification>

<success_criteria>
- Route locale.switch ada dengan method POST dan path /locale/{locale}
- LocaleController::switch() mengembalikan 404 untuk locale selain 'id' dan 'en'
- User::isKycVerified() → true hanya jika DB value 'verified' (bukan 'approved')
- getKycStatusAttribute() mengembalikan raw value tanpa transformasi
- User::registerMediaCollections() berisi `addMediaCollection('kyc_id_photo')->singleFile()` dan `addMediaCollection('kyc_selfie')->singleFile()` (untuk konsumsi Plan 04)
- php artisan test --filter LocaleSwitchTest: semua 4 test PASS
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/01-auth-infrastructure-fix/01-05-SUMMARY.md`
</output>
