---
phase: 01-auth-infrastructure-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Policies/ProductPolicy.php
  - app/Policies/OrderPolicy.php
  - app/Policies/SellerPolicy.php
  - app/Providers/AppServiceProvider.php
autonomous: true
requirements:
  - REQ-auth-rbac
  - REQ-nfr-security

must_haves:
  truths:
    - "Gate::allows('update', $product) mengembalikan true hanya untuk pemilik yang KYC verified"
    - "Gate::allows('view', $order) mengembalikan true untuk buyer dan seller yang punya item di order"
    - "Gate::allows('manage-seller-panel') mengembalikan true untuk user dengan role seller"
    - "Super admin bypass via Gate::before() sudah aktif"
    - "Policies terdaftar di AppServiceProvider::boot()"
  artifacts:
    - path: "app/Policies/ProductPolicy.php"
      provides: "Authorization rules untuk Product model"
      exports: ["viewAny", "view", "create", "update", "delete", "toggleActive"]
    - path: "app/Policies/OrderPolicy.php"
      provides: "Authorization rules untuk Order model"
      exports: ["view", "updateStatus", "dispute"]
    - path: "app/Policies/SellerPolicy.php"
      provides: "Non-model seller panel authorization"
      exports: ["manage", "accessPanel", "submitKyc"]
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Policy + Gate registration"
      contains: "Gate::policy(Product::class"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "app/Policies/ProductPolicy.php"
      via: "Gate::policy(Product::class, ProductPolicy::class)"
      pattern: "Gate::policy"
    - from: "app/Providers/AppServiceProvider.php"
      to: "Gate::before()"
      via: "super_admin bypass"
      pattern: "Gate::before"
---

<objective>
Buat 3 Policy class (ProductPolicy, OrderPolicy, SellerPolicy) dan daftarkan di AppServiceProvider dengan Gate::before() bypass untuk super_admin.

Purpose: REQ-auth-rbac — saat ini tidak ada Policy class sama sekali. Tanpa ini, semua `$this->authorize()` di controller akan lempar AuthorizationException bahkan untuk admin.
Output: 3 file Policy + AppServiceProvider yang di-update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-auth-infrastructure-fix/01-RESEARCH.md

<interfaces>
<!-- Dari app/Models/User.php -->
```php
// Method yang tersedia pada User untuk digunakan dalam Policy
public function isSeller(): bool     // true untuk seller/admin/super_admin
public function isAdmin(): bool      // true untuk admin/super_admin
public function isBuyer(): bool      // true untuk buyer/admin/super_admin
public function isKycVerified(): bool // checks kyc_status in ['verified', 'approved']
public function hasPlatformRole(array|string|UserRole $roles): bool

// PENTING: getKycStatusAttribute() mengubah 'verified' -> 'approved' saat dibaca.
// isKycVerified() checks both ['verified','approved'] — gunakan isKycVerified(), jangan cek raw value.
```

<!-- Dari app/Models/Order.php (diasumsikan berdasarkan schema) -->
```php
// Order memiliki:
//   buyer_id  — FK ke users.id
//   items()   — hasMany(OrderItem) di mana OrderItem::seller_id adalah FK ke users.id
// Order TIDAK memiliki seller_id langsung — seller diidentifikasi via order items
```

<!-- Dari app/Models/Product.php (diasumsikan berdasarkan schema) -->
```php
// Product memiliki seller_id — FK ke users.id
```

<!-- Arsitektur: AppServiceProvider di Laravel 12 — tidak ada AuthServiceProvider terpisah -->
// Gate::policy() didaftarkan di AppServiceProvider::boot()
// Gate::before() di-register SETELAH policy agar tidak short-circuit policy checks yang valid
// Tapi sesuai Laravel behavior: Gate::before() dipanggil SEBELUM policy — ini yang diinginkan untuk super_admin
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Buat ProductPolicy dan OrderPolicy</name>
  <files>app/Policies/ProductPolicy.php, app/Policies/OrderPolicy.php</files>
  <behavior>
    - ProductPolicy::create() → true hanya jika isSeller() && isKycVerified()
    - ProductPolicy::update() → true hanya jika isSeller() && isKycVerified() && user->id === product->seller_id
    - ProductPolicy::delete() → true hanya jika isSeller() && user->id === product->seller_id (tanpa KYC requirement)
    - ProductPolicy::toggleActive() → delegasi ke update()
    - ProductPolicy::viewAny() dan view() → return true (publik)
    - OrderPolicy::view() → true jika buyer_id match ATAU order->items()->where('seller_id', user->id)->exists() ATAU isAdmin()
    - OrderPolicy::updateStatus() → true jika seller punya item di order ATAU isAdmin()
    - OrderPolicy::dispute() → true hanya jika user adalah buyer DAN order status adalah 'delivered' atau 'processing'
  </behavior>
  <action>
Buat kedua file Policy. Tidak ada analog yang ada di codebase — gunakan Laravel Policy stub pattern.

**app/Policies/ProductPolicy.php**
```php
<?php

namespace App\Policies;

use App\Models\Product;
use App\Models\User;

class ProductPolicy
{
    public function viewAny(?User $user): bool
    {
        return true;
    }

    public function view(?User $user, Product $product): bool
    {
        return true;
    }

    public function create(User $user): bool
    {
        return $user->isSeller() && $user->isKycVerified();
    }

    public function update(User $user, Product $product): bool
    {
        return $user->isSeller()
            && $user->isKycVerified()
            && $user->id === $product->seller_id;
    }

    public function delete(User $user, Product $product): bool
    {
        return $user->isSeller()
            && $user->id === $product->seller_id;
    }

    public function toggleActive(User $user, Product $product): bool
    {
        return $this->update($user, $product);
    }
}
```

**app/Policies/OrderPolicy.php**
```php
<?php

namespace App\Policies;

use App\Models\Order;
use App\Models\User;

class OrderPolicy
{
    public function view(User $user, Order $order): bool
    {
        return $user->id === $order->buyer_id
            || $order->items()->where('seller_id', $user->id)->exists()
            || $user->isAdmin();
    }

    public function updateStatus(User $user, Order $order): bool
    {
        return $order->items()->where('seller_id', $user->id)->exists()
            || $user->isAdmin();
    }

    public function dispute(User $user, Order $order): bool
    {
        $allowedStatuses = ['delivered', 'processing'];

        return $user->id === $order->buyer_id
            && in_array($order->status instanceof \BackedEnum ? $order->status->value : $order->status, $allowedStatuses, true);
    }
}
```

PERHATIAN: `viewAny` dan `view` di ProductPolicy menggunakan `?User $user` (nullable) karena publik — tidak memerlukan login.
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php -l app/Policies/ProductPolicy.php && php -l app/Policies/OrderPolicy.php && echo "Syntax OK"</automated>
  </verify>
  <done>Kedua file ada; php -l tidak ada error; class names sesuai namespace App\Policies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Buat SellerPolicy dan update AppServiceProvider</name>
  <files>app/Policies/SellerPolicy.php, app/Providers/AppServiceProvider.php</files>
  <behavior>
    - SellerPolicy::manage() → true jika isSeller() (KYC check dilakukan middleware, bukan policy)
    - SellerPolicy::accessPanel() → true jika isSeller()
    - SellerPolicy::submitKyc() → true jika isSeller() && !isKycVerified() && kyc_status !== 'pending'
    - AppServiceProvider::boot() mendaftarkan Gate::policy untuk Product dan Order
    - Gate::define('manage-seller-panel') terdaftar untuk SellerPolicy::manage
    - Gate::define('submit-kyc') terdaftar untuk SellerPolicy::submitKyc
    - Gate::before() memberikan super_admin akses ke semua ability
  </behavior>
  <action>
**app/Policies/SellerPolicy.php**
```php
<?php

namespace App\Policies;

use App\Models\User;

class SellerPolicy
{
    public function manage(User $user): bool
    {
        return $user->isSeller();
    }

    public function accessPanel(User $user): bool
    {
        return $user->isSeller();
    }

    public function submitKyc(User $user): bool
    {
        return $user->isSeller()
            && ! $user->isKycVerified()
            && $user->getRawOriginal('kyc_status') !== 'pending';
    }
}
```

PERHATIAN di submitKyc: gunakan `getRawOriginal('kyc_status')` karena accessor `getKycStatusAttribute` mengubah 'verified'→'approved'. Kita cek nilai RAW 'pending' untuk mencegah double-submit.

**app/Providers/AppServiceProvider.php** — update boot() method:
```php
<?php

namespace App\Providers;

use App\Models\Order;
use App\Models\Product;
use App\Policies\OrderPolicy;
use App\Policies\ProductPolicy;
use App\Policies\SellerPolicy;
use Illuminate\Support\Facades\Gate;
use Illuminate\Support\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    public function register(): void
    {
        //
    }

    public function boot(): void
    {
        // Daftarkan policy untuk model
        Gate::policy(Product::class, ProductPolicy::class);
        Gate::policy(Order::class, OrderPolicy::class);

        // Non-model abilities untuk seller panel
        Gate::define('manage-seller-panel', [SellerPolicy::class, 'manage']);
        Gate::define('access-seller-panel', [SellerPolicy::class, 'accessPanel']);
        Gate::define('submit-kyc', [SellerPolicy::class, 'submitKyc']);

        // Super admin bypass — dipanggil sebelum semua policy check
        // Menggunakan 'super_admin' sebagai Spatie role name (sesuai UserRole::SUPER_ADMIN)
        Gate::before(function (User $user, string $ability): ?bool {
            if ($user->hasPlatformRole('super_admin')) {
                return true;
            }

            return null; // Lanjutkan ke policy normal
        });
    }
}
```

PERHATIAN: `Gate::before()` harus di-register SEBELUM `Gate::policy()` di-call tidak masalah — Laravel menjalankan `before` callbacks sebelum policy. Return `null` (bukan `false`) dari callback agar policy normal tetap dievaluasi untuk non-super-admin.

Tambahkan `use App\Models\User;` di import karena digunakan dalam closure Gate::before.
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php -l app/Policies/SellerPolicy.php && php artisan config:clear && php artisan route:list --name=login 2>&1 | grep -c "auth/login" && echo "AppServiceProvider loaded OK"</automated>
  </verify>
  <done>SellerPolicy.php ada; AppServiceProvider::boot() memiliki Gate::policy calls dan Gate::before; php artisan config:clear berjalan tanpa exception yang berarti AppServiceProvider valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Gate::check | User-supplied model ID bisa digunakan untuk akses model milik orang lain |
| Policy::dispute() | Status order dikirim dari request atau DB — harus dari DB saja |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Elevation of Privilege | ProductPolicy::update | mitigate | Check seller_id === user->id memastikan ownership sebelum izinkan update |
| T-01-02-02 | Elevation of Privilege | OrderPolicy::view via items() | mitigate | Query DB langsung (`items()->where('seller_id')`) tidak mengandalkan user input |
| T-01-02-03 | Elevation of Privilege | Gate::before() super_admin | accept | Super admin legitimate role — bypass disengaja, log admin actions via spatie/activitylog di fase berikutnya |
| T-01-02-04 | Tampering | SellerPolicy::submitKyc double-submit | mitigate | Cek `kyc_status !== 'pending'` mencegah pengiriman ulang saat sedang review |
</threat_model>

<verification>
```bash
# Semua policy file ada
ls /Users/tokaf/gamecommerce/app/Policies/

# Syntax check
cd /Users/tokaf/gamecommerce
php -l app/Policies/ProductPolicy.php
php -l app/Policies/OrderPolicy.php
php -l app/Policies/SellerPolicy.php
php -l app/Providers/AppServiceProvider.php

# AppServiceProvider dapat di-load
php artisan config:clear 2>&1 | grep -v "^$"

# Jalankan policy tests (memerlukan Plan 02 selesai)
php artisan test --filter "ProductPolicyTest|OrderPolicyTest|SellerPolicyTest"
```
</verification>

<success_criteria>
- 3 file Policy ada di app/Policies/
- AppServiceProvider::boot() berisi Gate::policy() untuk Product dan Order
- Gate::define('manage-seller-panel') dan Gate::define('submit-kyc') terdaftar
- Gate::before() mengembalikan true untuk super_admin dan null untuk user lain
- php artisan config:clear tidak melempar exception
- php artisan test --filter ProductPolicyTest: semua 4 test PASS
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/01-auth-infrastructure-fix/01-02-SUMMARY.md`
</output>
