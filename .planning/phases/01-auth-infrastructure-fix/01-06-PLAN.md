---
phase: 01-auth-infrastructure-fix
plan: 06
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - tests/Feature/Auth/AuthRoutesTest.php
autonomous: false
requirements:
  - REQ-auth-routes
  - REQ-auth-register
  - REQ-auth-login
  - REQ-auth-2fa
  - REQ-auth-rbac
  - REQ-auth-kyc
  - REQ-nfr-security
  - REQ-nfr-localization

must_haves:
  truths:
    - "Semua Fortify named routes resolve tanpa 404"
    - "Route seller.kyc.verify ada dan EnsureKycVerified tidak redirect loop"
    - "Banned user di-logout dan di-redirect saat akses web"
    - "Policies terdaftar dan Gate::check bekerja untuk Product + Order"
    - "POST /locale/{locale} update session"
    - "php artisan test berjalan tanpa failure"
  artifacts:
    - path: "tests/Feature/Auth/AuthRoutesTest.php"
      provides: "Route registration smoke test"
      contains: "seller.kyc.verify"
  key_links:
    - from: "Gate::check('update', $product)"
      to: "app/Policies/ProductPolicy.php"
      via: "AppServiceProvider::boot() Gate::policy()"
      pattern: "ProductPolicy"
    - from: "EnsureKycVerified::handle()"
      to: "route('seller.kyc.verify')"
      via: "Route::has('seller.kyc.verify')"
      pattern: "seller.kyc.verify"
---

<objective>
Wave 2 — verifikasi penuh semua implementasi dari Wave 1. Buat AuthRoutesTest untuk smoke test route registration, jalankan seluruh suite test, dan lakukan human verification untuk flow interaktif (KYC form, locale switch).

Purpose: Fase ini adalah infrastruktur — lebih baik mengetahui ada yang rusak sekarang daripada saat membangun fitur di fase berikutnya.
Output: AuthRoutesTest.php + hasil test suite + checkpoint human-verify untuk visual QA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-auth-infrastructure-fix/01-01-SUMMARY.md
@.planning/phases/01-auth-infrastructure-fix/01-02-SUMMARY.md
@.planning/phases/01-auth-infrastructure-fix/01-03-SUMMARY.md
@.planning/phases/01-auth-infrastructure-fix/01-04-SUMMARY.md
@.planning/phases/01-auth-infrastructure-fix/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat AuthRoutesTest dan jalankan full test suite</name>
  <files>tests/Feature/Auth/AuthRoutesTest.php</files>
  <action>
Buat file test yang memverifikasi semua Fortify named routes dan custom routes terdaftar dengan benar.

**tests/Feature/Auth/AuthRoutesTest.php**
```php
<?php

namespace Tests\Feature\Auth;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Route;
use Tests\TestCase;

class AuthRoutesTest extends TestCase
{
    use RefreshDatabase;

    /**
     * Semua Fortify routes yang wajib ada (prefix auth/).
     */
    public function test_fortify_named_routes_are_registered(): void
    {
        $requiredRoutes = [
            'login',
            'logout',
            'register',
            'password.request',
            'password.email',
            'password.reset',
            'password.update',
            'verification.notice',
            'verification.verify',
            'verification.send',
            'two-factor.login',
            'two-factor.enable',
            'two-factor.disable',
            'two-factor.confirm',
            'password.confirm',
        ];

        foreach ($requiredRoutes as $routeName) {
            $this->assertTrue(
                Route::has($routeName),
                "Route [{$routeName}] tidak terdaftar. Cek FortifyServiceProvider."
            );
        }
    }

    public function test_fortify_login_route_resolves_to_auth_prefix(): void
    {
        $loginUrl = route('login');
        $this->assertStringContainsString('/auth/login', $loginUrl);
    }

    public function test_fortify_logout_route_is_post_method(): void
    {
        // Verifikasi logout adalah POST, bukan GET (bug klasik)
        $routes = Route::getRoutes();
        $logoutRoute = $routes->getByName('logout');
        $this->assertNotNull($logoutRoute, 'Route logout tidak ditemukan');
        $this->assertContains('POST', $logoutRoute->methods());
    }

    public function test_seller_kyc_verify_route_is_registered(): void
    {
        // KRITIS: EnsureKycVerified middleware bergantung pada route ini
        $this->assertTrue(
            Route::has('seller.kyc.verify'),
            "Route [seller.kyc.verify] tidak terdaftar. EnsureKycVerified akan redirect loop."
        );
    }

    public function test_seller_kyc_store_route_is_registered(): void
    {
        $this->assertTrue(
            Route::has('seller.kyc.store'),
            "Route [seller.kyc.store] tidak terdaftar. KYC form submission tidak bisa diproses."
        );
    }

    public function test_locale_switch_route_is_registered(): void
    {
        $this->assertTrue(
            Route::has('locale.switch'),
            "Route [locale.switch] tidak terdaftar."
        );
    }

    public function test_login_page_returns_200(): void
    {
        $response = $this->get(route('login'));
        $response->assertStatus(200);
    }

    public function test_register_page_returns_200(): void
    {
        $response = $this->get(route('register'));
        $response->assertStatus(200);
    }

    public function test_two_factor_challenge_page_returns_200(): void
    {
        $response = $this->get(route('two-factor.login'));
        $response->assertStatus(200);
    }

    public function test_logout_get_request_is_not_found_or_not_allowed(): void
    {
        // GET /auth/logout tidak boleh ada (security: CSRF bypass via link click)
        $response = $this->get('/auth/logout');
        $this->assertContains($response->status(), [404, 405]);
    }
}
```

Setelah membuat file, jalankan full test suite dan report hasilnya:
```bash
cd /Users/tokaf/gamecommerce && php artisan test 2>&1
```

Jika ada test failure, analisis error message dan perbaiki sebelum lanjut ke checkpoint.

**Checklist pre-checkpoint:**
- [ ] php artisan test -- semua test PASS
- [ ] php artisan route:list --name=login menampilkan route
- [ ] php artisan route:list --name=seller.kyc menampilkan 2 routes
- [ ] grep -c "EnsureNotBanned" bootstrap/app.php → minimal 1
- [ ] php artisan config:clear tidak error
  </action>
  <verify>
    <automated>cd /Users/tokaf/gamecommerce && php artisan test --filter AuthRoutesTest 2>&1 | tail -10</automated>
  </verify>
  <done>AuthRoutesTest semua test PASS; php artisan test (full suite) tidak ada failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Wave 1 selesai — implementasi lengkap:
1. 10 test files di tests/Feature/
2. 3 Policies + AppServiceProvider (Gate registration)
3. EnsureNotBanned middleware + bootstrap/app.php registration
4. KycController + KycSubmissionRequest + KYC routes + Blade view
5. LocaleController + locale route
6. User::isKycVerified() refactor (hapus 'verified'→'approved' mapping)
  </what-built>
  <how-to-verify>
Lakukan verifikasi berikut secara berurutan:

**1. Route smoke test (terminal):**
```bash
cd /Users/tokaf/gamecommerce
php artisan route:list --name=login
php artisan route:list --name=seller.kyc
php artisan route:list --name=locale.switch
```
Expected: Ketiga perintah menampilkan route yang valid.

**2. Full test suite:**
```bash
php artisan test
```
Expected: Semua test PASS, tidak ada FAILED atau ERROR.

**3. App boots (tidak ada exception):**
```bash
php artisan config:clear && php artisan route:cache && php artisan route:clear
```
Expected: Tidak ada exception.

**4. Visual check — KYC form:**
```bash
php artisan serve
```
Buka browser: http://localhost:8000/seller/kyc (login sebagai seller dulu)
Expected: Form KYC tampil dengan 4 section: Data Identitas, Dokumen Foto, Rekening Bank, NPWP.

**5. Visual check — ban redirect:**
Buat user banned via tinker:
```bash
php artisan tinker
$user = App\Models\User::first(); // atau buat baru
$user->update(['is_banned' => true]);
```
Login sebagai user tersebut, akses homepage.
Expected: Di-logout dan redirect ke /auth/login dengan flash message error.

**6. Locale switch:**
Di browser, buka halaman apapun dan kirim POST request:
```bash
curl -X POST http://localhost:8000/locale/en -b "laravel_session=..." -H "X-CSRF-TOKEN: ..."
```
Atau buat form test di browser dengan hidden field locale=en.
Expected: Session locale berubah ke 'en'.
  </how-to-verify>
  <resume-signal>
Ketik "approved" jika semua verifikasi passed, atau deskripsi issue yang ditemukan untuk perbaikan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → production routes | Test suite verifikasi seluruh middleware stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | Route cache poisoning | mitigate | php artisan route:clear setelah route:cache memastikan cache fresh; CI akan selalu jalankan kedua command |
| T-01-06-02 | Denial of Service | Test suite memerlukan DB | accept | RefreshDatabase ensures isolation; SQLite in-memory untuk test env (config/database.php) |
</threat_model>

<verification>
```bash
# Comprehensive verification
cd /Users/tokaf/gamecommerce

# 1. Semua route ada
php artisan route:list --name=login | grep -c "auth/login"
php artisan route:list --name=seller.kyc | grep -c "kyc"
php artisan route:list --name=locale.switch | grep -c "locale"

# 2. Ban middleware terdaftar
grep -v '^#' bootstrap/app.php | grep -c "EnsureNotBanned"

# 3. Policy registration
grep -c "Gate::policy" app/Providers/AppServiceProvider.php

# 4. Full test suite
php artisan test

# 5. KYC refactor
php artisan tinker --execute="echo App\Models\User::factory()->make(['kyc_status'=>'verified'])->isKycVerified() ? 'PASS' : 'FAIL';"
```
</verification>

<success_criteria>
- php artisan test: 0 failures, 0 errors
- AuthRoutesTest: 10 test PASS
- Route seller.kyc.verify terdaftar dan EnsureKycVerified tidak redirect loop
- User::isKycVerified() benar untuk 'verified' (true) dan 'approved' (false sekarang)
- Ban flow: banned user di-logout dan redirect ke login
- Semua requirement Phase 1 terverifikasi via automated tests
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/01-auth-infrastructure-fix/01-06-SUMMARY.md`
</output>
